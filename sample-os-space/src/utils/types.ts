export type WidgetType = {
  id: string;
  icon: string;
  title: string;
  description: string;
  metricId: string;
};

export type MetricType = {
  id: string;
  icon: string;
  title: string;
  type: string;
};
